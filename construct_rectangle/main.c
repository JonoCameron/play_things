#include <stdio.h>

/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
int* constructRectangle(int area, int* returnSize){
    int ;

    //printf("hello, %d, %d", arr[0], arr[1]);
    return 0;
}

int main(){
    int area = 42;
    int arr[2];

    arr[0] = 1;
    arr[1] = 2;

    constructRectangle(area, arr);
    printf("hello, %d, %d", arr[0], arr[1]);
    return 0;
}