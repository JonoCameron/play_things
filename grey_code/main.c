#include <stdio.h>

/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
int* greyCode(int n, int* returnSize){
    // do nothing 
}

int main(){
    int n = 2;


    printf("Too hard for now lol\n");
    return 0;
}