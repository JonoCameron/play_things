#include "stdio.h"
#include "stdlib.h"

int** generate(int numRows, int* returnSize, int** returnColumnSizes);
int sum(int numRows, int size);

int main(argc, argv){



    return 0;
}

int** generate(int numRows, int* returnSize, int** returnColumnSizes){
    
};

int sum(int numRows, int size){
    
    if(numRows != 0){
        size = size + (numRows - 1);
        
        
    }
}
